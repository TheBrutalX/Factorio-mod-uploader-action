name: Release

on:
  push:
    tags:
      - 'v*'
      - '[0-9]+.[0-9]+.[0-9]+'
    paths-ignore: 
      - '**.md'

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:

      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm test

  publish:
    runs-on: ubuntu-latest
    needs: [ tests ]
    permissions:
      contents: write

    steps:

      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Set up Git
        run: |
          git config user.name 'GitHub Actions'
          git config user.email 'actions@github.com'

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Get version from tag
        id: version
        run: |
          # Extract version, handling both 'v1.0.0' and '1.0.0' formats
          VERSION=${GITHUB_REF_NAME#v}
          echo "Version: $VERSION"
          echo "VERSION=$VERSION" >> $GITHUB_OUTPUT

      - name: Create GitHub Release
        env:
          VERSION: ${{ steps.version.outputs.VERSION }}
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: Release v${{ env.VERSION }}
          generate_release_notes: true

      - name: Commit bundle.js (if changed)
        run: |
          git add -f dist/
          git add package-lock.json
          if ! git diff --cached --quiet; then
            git commit -m 'chore(release): Added bundle.js [skip ci]'
            git push
          fi

      - name: Move latest tag to newest release
        env:
          VERSION: ${{ steps.version.outputs.VERSION }}
        run: |
          git tag -d latest
          git push origin :latest
          git tag -a latest -m "Latest release: ${{ github.ref_name }}"
          git push origin latest
